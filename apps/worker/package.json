{
  "name": "@my-digital/worker",
  "version": "0.0.1-foundation",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"@my-digital/worker: lint not configured yet\""
  },
  "dependencies": {
    "@my-digital/core": "workspace:*",
    "@my-digital/envelope": "workspace:*",
    "@my-digital/payments-stripe": "workspace:*",
    "@my-digital/server-core": "workspace:*",
    "@my-digital/store": "workspace:*",
    "@my-digital/types": "workspace:*",
    "hono": "^4.6.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.0.0"
  }
}
