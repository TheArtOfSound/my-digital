{
  "name": "@my-digital/store",
  "version": "0.0.1-foundation",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "generate": "drizzle-kit generate",
    "lint": "echo \"@my-digital/store: lint not configured yet\""
  },
  "dependencies": {
    "@my-digital/types": "workspace:*",
    "better-sqlite3": "^12.2.0",
    "drizzle-orm": "^0.44.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.5",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
