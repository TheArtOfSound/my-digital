{
  "name": "@my-digital/envelope",
  "version": "0.0.1-foundation",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"@my-digital/envelope: lint not configured yet\""
  },
  "dependencies": {
    "@my-digital/core": "workspace:*",
    "@my-digital/types": "workspace:*",
    "@noble/ciphers": "^1.2.1",
    "@noble/hashes": "^1.7.1",
    "libsodium-wrappers-sumo": "^0.7.15"
  },
  "devDependencies": {
    "@bryan237l/qev-cli": "^0.30.0",
    "@types/libsodium-wrappers-sumo": "^0.7.8",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
