{
  "name": "@my-digital/payments-stripe",
  "version": "0.0.1-foundation",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"@my-digital/payments-stripe: lint not configured yet\""
  },
  "dependencies": {
    "@my-digital/core": "workspace:*",
    "@my-digital/types": "workspace:*",
    "stripe": "^18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
